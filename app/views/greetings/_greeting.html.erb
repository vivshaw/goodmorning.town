<tr id="<%= dom_id greeting %>" data-controller="streamElement">
  <td>good morning <%= greeting.word %> town <%= greeting.id %></td>
  <td><%= link_to 'Destroy', greeting, method: :delete, data: { confirm: 'Are you sure?' } %></td>
</tr>
